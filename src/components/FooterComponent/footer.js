import React, { Component } from "react";

export default class Footer extends Component {
    state = {
        fields: {}
    };

    render() {
        return (
            <footer>
            </footer>
        );
    }
}

